apiVersion: v1
kind: Service
metadata:
  name: haven-api
  namespace: haven-system
  labels:
    app: haven-api
    haven.io/component: api
spec:
  selector:
    app: haven-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
