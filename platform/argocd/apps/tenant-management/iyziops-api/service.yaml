apiVersion: v1
kind: Service
metadata:
  name: iyziops-api
  namespace: haven-system
  labels:
    app: iyziops-api
    haven.io/component: api
spec:
  selector:
    app: iyziops-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
