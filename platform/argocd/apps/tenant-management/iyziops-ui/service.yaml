apiVersion: v1
kind: Service
metadata:
  name: iyziops-ui
  namespace: haven-system
  labels:
    app: iyziops-ui
    haven.io/component: ui
spec:
  selector:
    app: iyziops-ui
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
