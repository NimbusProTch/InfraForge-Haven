apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: haven-tenant-apps
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: https://github.com/NimbusProTch/InfraForge-Haven.git
        revision: main
        directories:
          - path: "gitops/tenants/*/*"
          # Exclude services subdirectories
          - path: "gitops/tenants/*/services"
            exclude: true
          - path: "gitops/tenants/*/services/*"
            exclude: true
  template:
    metadata:
      # segments: [0]=gitops [1]=tenants [2]=tenant [3]=app
      name: "{{ index .path.segments 2 }}-{{ index .path.segments 3 }}"
      namespace: argocd
      labels:
        haven.io/managed: "true"
        haven.io/tenant: "{{ index .path.segments 2 }}"
        haven.io/app: "{{ index .path.segments 3 }}"
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/NimbusProTch/InfraForge-Haven.git
        targetRevision: main
        path: "charts/haven-app"
        helm:
          valueFiles:
            - "/{{ .path.path }}/values.yaml"
      destination:
        server: https://kubernetes.default.svc
        namespace: "tenant-{{ index .path.segments 2 }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
          - ServerSideApply=true
        retry:
          limit: 3
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
