# Sprint — Haven 15/15 (multi-AZ + private networking)

**Tarih:** 2026-04-14  
**Branch:** `feat/haven-15-15`  
**Önceki baseline:** 13/15 (fail: `infraMultiAZ`, `privatenetworking`)  
**Hedef:** 15/15

## Kapsam

Cluster baseline'ı iki Haven kontrolünde takılıydı:
- `infraMultiAZ` — tüm node'lar `fsn1-dc14` zone'unda
- `privatenetworking` — her node'da public IPv4 (`.status.addresses[type=ExternalIP]`)

Bu iki problem cluster içinden çözülemezdi; IaC + cluster rebuild gerekti.

## Değişiklikler

### Infra

- `infrastructure/modules/hetzner-infra/variables.tf` — `worker_location` değişkeni (fsn1/nbg1/hel1).
- `infrastructure/modules/hetzner-infra/main.tf` — `hcloud_firewall.this` kaldırıldı (cluster nodes public IP'siz, shared firewall'ın hedefi yok).
- `infrastructure/modules/hetzner-infra/nat.tf` (yeni) — `hcloud_firewall.nat` + `hcloud_server.nat` (cx22, public IPv4) + `hcloud_server_network.nat` (`.254`) + `hcloud_network_route.default_via_nat` (`0.0.0.0/0 → .254`).
- `infrastructure/modules/hetzner-infra/templates/nat-cloud-init.yaml.tpl` (yeni) — `ip_forward` + systemd oneshot MASQUERADE rule (iptables-persistent yok, idempotent).
- `infrastructure/modules/hetzner-infra/outputs.tf` — `firewall_id` silindi; `nat_public_ipv4`, `nat_private_ipv4`, `network_route_id`, `worker_zone` eklendi.
- `infrastructure/environments/prod/variables.tf` — `worker_location` eklendi.
- `infrastructure/environments/prod/prod.auto.tfvars` — `worker_location = "nbg1"`.
- `infrastructure/environments/prod/hetzner.tf`:
  - `module.hetzner_infra` çağrısına `worker_location = var.worker_location`.
  - `hcloud_server.master/worker` → `public_net { ipv4_enabled = false; ipv6_enabled = false }`, `firewall_ids` kaldırıldı.
  - `hcloud_server.worker.location = var.worker_location` (nbg1).
- `infrastructure/environments/prod/outputs.tf` — stale `first_master_public_ipv4`, `master_public_ipv4s`, `worker_public_ipv4s` kaldırıldı; `nat_public_ipv4`, `worker_zone` pass-through eklendi.
- `infrastructure/modules/rke2-cluster/templates/rke2-config.yaml.tpl` — `node-external-ip` satırı ve `tls-san` listesinden `__PUBLIC_IP__` kaldırıldı.
- `infrastructure/modules/rke2-cluster/templates/rke2-config-worker.yaml.tpl` — aynısı.
- `infrastructure/modules/rke2-cluster/templates/{master,joining-master,worker}-cloud-init.yaml.tpl`:
  - `curl http://169.254.169.254/hetzner/v1/metadata/public-ipv4` ve `__PUBLIC_IP__` sed substitution kaldırıldı.
  - Yeni `bootcmd:` — `timeout 3 bash -c '</dev/tcp/1.1.1.1/443'` ile NAT egress hazır olana kadar 5 dk'ya kadar bekliyor (apt package install öncesinde).
- `infrastructure/modules/rke2-cluster/manifests/rke2-cilium-config.yaml.tpl` — `mtu: 1400` (Hetzner private network 1450 - VXLAN 50 = 1400).

### API / tests

- `api/tests/test_haven_compliance_assertions.py` (yeni) — 10 statik test; IaC template'lerini + fixture node JSON'larını parse ederek rejection guards yazıyor.
- `api/tests/fixtures/sample_nodes_haven_passing.json`, `sample_nodes_single_zone.json`, `sample_nodes_with_external_ip.json` (yeni).

### Platform

- `README.md` (yeni) — VNG Haven 15/15 static shields.io badge + `make haven` quick start.
- `CLAUDE.md` — Aktif Gotcha'lar bölümüne 2 yeni not: "kubelet flag yetmez, CCM'yi düşür" + "workers farklı DC'de".
- `Makefile`:
  - `api-test*` hedefleri `$(PYTHON) = api/.venv/bin/python` ile çalışıyor (sistem `python` symlink bağımlılığı yok).
  - `infra-*` hedefleri `infrastructure/environments/prod`'a yönlendirildi (eski `dev` pathi ölü); `infra-init`, `infra-plan`, `infra-apply`, `infra-destroy`, `kubeconfig` eklendi.
- `scripts/fetch-kubeconfig.sh` (yeni) — NAT ProxyJump ile master private IP'den rke2.yaml çeker, server URL'ini API LB public IP ile rewrite eder, `/tmp/iyziops-kubeconfig`'e pin'ler.

## Test sayısı

**Öncesi:** 1354  
**Sonrası:** 1364 (+10)

## Acceptance (Phase E)

- [ ] `tofu apply` hands-off 25-30 dk
- [ ] 6 node Ready (3 master fsn1-dc14, 3 worker nbg1-dc3)
- [ ] Hiçbir node'da `.status.addresses[type=ExternalIP]` yok
- [ ] `make haven` → 15 PASS / 0 FAIL
- [ ] 16 ArgoCD app Synced+Healthy
- [ ] `make api-test` — 1361 passed, 3 skipped

## Riskler / notlar

- NAT box SPOF (dev kabul edilebilir, prod'da HA pair gelecek).
- Cross-DC cilium trafik ~15ms overhead (kubelet→apiserver, ingress LB→worker). Yazılımsal etkisi yok.
- Hetzner CCM'in otomatik `topology.kubernetes.io/zone` label'ı basması Hetzner CCM upstream davranışı — doğrulama Phase D'de.
- `bootcmd` TCP probe 1.1.1.1:443'e dayalı; eğer Cloudflare offline (unlikely), cluster boot fallback'le continue ediyor.
